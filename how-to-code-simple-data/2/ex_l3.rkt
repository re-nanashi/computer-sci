;; SeatNum is Integer[1, 32]
;; interp. seat numbers in a row, 1 and 32 are aisle seats
(define seatNumber1 1)  ;aisle
(define seatNumber2 12) ;middle
(define seatNumber3 32) ;aisle

(define (seatNumberFunc sn) 
  (...sn))

;; Template rules used:
;; - atomic non-distinct: Integer[1, 32]
