;; LetterGrade is one of:
;; - "A"
;; - "B"
;; - "C"
;; interp. letter grade in a course

(define (letterGradeFunc lg)
  (cond [(string=? "A" lg) (...)]
        [(string=? "B" lg) (...)]
        [(string=? "C" lg) (...)]))

;; Template rules used:
;; - one of: 3 cases
;; - atomic distinct: "A"
;; - atomic distinct: "B"
;; - atomic distinct: "C"
