CONSTANTS:
lambda image
height
width = 3 x height
ctr y

rotation speed
moving speed

pi
radius of the circle

CHANGING:
position across the x axis
angle rotation
direction


OPTIONS:
on tick
to draw

Data definitions:
Lambda has position across x axis, rotation, direction


Functions:
on tick

next_lambda
if (x== (width - radius))
    dir = left
if (x == (0 + radius))
    dir = right
if (dir is right)
    increment
if (dir is left)
    decrement

render_lambda



